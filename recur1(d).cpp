/*
Recursive function to check if a string is palindrome
Given a string, write a recursive function that check if the given string is palindrome,
else not palindrome.
Examples :
Input : malayalam
Output : Yes
*/
#include<iostream>
#include<cstring>
using namespace std;
bool check_rev(char s[],int f,int l)
{
	if(s[])
}
int main()
{
	string word;
	cin>>word;
	n=str.length(word);
	if(check_rev(word,0,n-1)==true)
	cout<<"Yes";
	else
	cout<<"No";
	return 0;
	
}
